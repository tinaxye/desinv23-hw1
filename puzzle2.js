function repeat(direction, max) {
  var count = 0;
  while (count < max) {
    direction;
    count = count + 1;
  }
}

repeat(down, 6);
repeat(right, 3);
repeat(up, 2);
repeat(right, 2);
repeat(down, 3);
